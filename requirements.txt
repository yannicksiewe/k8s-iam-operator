# Core dependencies
# Pinned (not >=): kopf 1.44.x / kubernetes 36.x regressed in-cluster auth
# (kopf aborts startup with "Ran out of valid credentials" after a transient
# connection error). kopf 1.38.0 + kubernetes 35.0.0 are the last known-good
# pairing (shipped in v4.1.x). Unpinned ranges made builds non-reproducible.
kopf==1.38.0
kubernetes==35.0.0
flask>=2.3.0
gunicorn>=21.0.0

# Security: pin urllib3 to a patched release (CVE-2026-44431/44432).
# Pulled transitively by kubernetes/opentelemetry; pinned here to satisfy the
# image vulnerability scan.
urllib3>=2.7.0

# Metrics
prometheus_client>=0.17.0

# Tracing (OpenTelemetry)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation>=0.41b0
opentelemetry-exporter-otlp>=1.20.0
grpcio>=1.50.0
